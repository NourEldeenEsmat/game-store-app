spring:
  application:
    name: game-store
  data-source:
    url: jdbc:mysql://localhost:3306/game_store_db?useSSL=false&serverTimezone=UTC
    username: root
    password: racefour5
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
